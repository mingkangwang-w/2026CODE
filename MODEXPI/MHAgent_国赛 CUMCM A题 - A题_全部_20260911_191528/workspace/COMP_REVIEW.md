# 逻辑对抗复核报告（comp-review）

> 独立视角，只挑"数值合法但逻辑错"的硬伤。锚点：`constraint_audit.py` 已裁 27 PASS / 0 FAIL / 1 DEVIATION / 2 DEFER；`method_claims_check.py` 8/8；本步不重复其确定性结论，只兜"确定性查不了的语义残余"。

## 结论速览

**fatal_count = 0，可放行。** 无方向反、无重复计量、无跨问硬矛盾、无任务读歪。
2 条 major/minor（Q4 温度侧 Robin 残差越逐字上界、平台延拓属情景外推）不硬拦，但论文须如实标注，禁确定性口吻。

---

## 逐类对撞（六类缺口）

### 1. 方向/界反没反 —— ✅ 通过
- 终点判据 `max_r C < 0.15`（`problem_3.py:266/388`、`problem_4.py:421`、`main.py:353`），**不是** `C.mean()`。题面"各处水分浓度应低于 0.15"= 逐点全满足，方向正确。
- 代入验证不等号：C 沿 r 单调递减、中心最湿（RESULTS 载 argmax_r C=0，对称性偏差 8e-15），故 `max_r C = C(0,t)`，判据等价 `C(0,t*)<0.15`；t* 处 max_r C=0.149999，方向与量级自洽。
- D 的分数指数 `exp(-0.89/C)`（`params.py:75-77/174-175`），源码不存在 `exp(-0.89*C)` 乘积形式。分数解 → C↓则D↓（越干越难脱水），方向符合结合水物理；乘积解方向相反已被三重佐证否掉。

### 2. 重复计量 —— ✅ 通过
- "总量=A+B"型隐患点为能量方程潜热汇项：题面未给 h_fg，代码能量方程源项为 0（HC-07），未把蒸发潜热既计入边界失水又计入体源，无双算。
- 质量守恒校验用"累计表面流出通量 vs 域内水分减少量"（fvkernel），是同一份通量的两侧对撞而非把边界流出再加一次到域内，非重复计量。

### 3. 外推口吻过硬 —— ⚠ minor（须论文标注）
- 附件 1 只到 14400 s，t>14400 s 用平台常值延拓（T_inf=49.9989 ℃、C_inf=0.04999），依据是 t≥10800 s 实测平台（std 0.153 ℃）。**属有数据支撑的情景延拓，不是纯臆造**，但问题 3/4 的 t*（57.27 h / 51.04 h）大部分时程落在观测区间外。
- 建议：论文中 t* 结论须标"基于平台恒温延拓的情景模拟，长时程边界需现场标定"，不得写成"实测确定的烘干时长"。附件 2 的 67.0 h 半径平台可作独立旁证，已用作交叉验证，口径合适。

### 4. 漏真实变量 —— ✅ 通过
- DATA_FACTS 中 role=observed 的关键量：附件 1（T_inf、C_inf 逐 60 s）全量插值入 Robin 边界；附件 2（R(t) 145 行）作为 Q4 动域上界；几何/初值/两组物性系数全部进模型。
- T(r,t)、C(r,t) 内部场 has_data=false（题目未给实测），已正确未编造真值、未做 RMSE/MAPE（HC-14）。无被"当不重要"丢掉的观测量。

### 5. 跨问矛盾 —— ✅ 通过
- Q3=57.27 h、Q4=51.04 h，"D 更小却更快"的表观矛盾已被定量拆解：冻结半径对照 Q4=129.35 h（D 变小效应），收缩把特征时间∝R² 压到 39.4%（加速 2.54×），净得 Q4 略快。方向有物理解释，非算错，与 PROBLEM_ANALYSIS §4.2 预判（Q4 略快）一致。
- Q4 求解域上界始终用附件 2 实测 R(t)，§6.3 的守缩预测通道只做校验不回灌，无"预测半径与实测半径互相污染"的循环论证。

### 6. 任务理解 vs 题目原文 —— ✅ 通过（独立重读题面原句）
- 求时长而非其他量：S19/S23 求"烘干所需时间/时长"，代码交付标量 t*(h)，未做成求别的量。
- 终点判据"各处"= max 而非 mean（S18），未反。
- 表 6 最外列"药材表面"= 随时间移动的 r=R(t)（HC-20），未误当固定 2 cm。
- 问题 1 用附录 2 常物性、问题 2/3 用附录 3、问题 4 用附录 4，未混用（`params.py:78-79`，各 problem_N.py 头部锁定）。
- 四问均未被擅自升级为优化/能耗/潜热/薄层经验模型（升级判定表逐项否决）。未见"求 A 做成求 B"或 min↔max 方向反。

---

## 偏离登记（如实报出，非放行）

- **B-06 / Q4 温度侧 Robin 残差**：实测 rel_res_T=1.213e-3，超逐字上界 1e-3（Q4 自身口径 2e-3 内）。已记 DEVIATION 非 PASS；达标条件：n_cells 提到约 89 可压到 1e-3。属数值精度 minor，不影响 t* 量级（cp±20% 仅动 t* 3.6e-4），论文验证章须如实标此残差与达标路径。
- **B-13/B-30 DEFER**：仅因论文正文尚未撰写，代码侧结论已定，非逻辑错。

## 诚实认知（天花板）
本复核与答题同源，"任务理解"类可能共盲区。已带题面原句逐条重读 S8/S14/S19/S22/S23/S24，未发现读歪；但对"分数指数/开尔文/仅径向收缩"三处定案沿用既有证据链，未再独立复现全部数值。机器可判部分由 logic_audit/constraint_audit 确定性拦截，此处结论均给了代码行/数值支撑。
