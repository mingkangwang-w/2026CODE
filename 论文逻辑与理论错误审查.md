# 《药材热风烘干过程的热-质耦合传递模型与收缩动边界烘干时长研究》

## 论文逻辑与理论错误审查报告

审查对象：`论文.docx`  
对照材料：题目原文 `A题.pdf`、附件1和附件2、论文正文中的表格与公式。

## 一、结论摘要

论文目前存在会影响核心结论有效性的理论和逻辑问题，主要集中在：

1. 问题三的含水率阈值前后不一致（1.5 与 0.15 kg/kg）。
2. 温度离散公式漏掉 `ρcp` 分母。
3. 变含水率介质的能量方程没有严格按焓守恒建立。
4. 干基含水率的质量守恒方程、总水量定义和收缩模型彼此不一致。
5. 移动边界问题中固相运动、物质坐标变换和边界通量的推导不充分，不能据此断言收缩项“精确相消”。
6. 守恒残差、灵敏度分析和数值结果缺乏可复核的完整证据。
7. 题目要求的 `result2.xlsx`、`result3.xlsx`、`result4.xlsx` 不存在，现有 `result1.xlsx` 仍是空模板样式。

因此，论文给出的问题三、问题四烘干时长（57.2745 h、51.0444 h）目前不能视为已被理论模型充分验证的可靠结果。

---

## 二、严重错误（必须优先修改）

### 1. 烘干阈值前后矛盾

**位置：**“一、问题重述”1.2 问题三；“6.3.1 模型与终点判据”。

论文在问题重述中写“各处水分浓度均低于 **1.5 kg/kg**”，但后文定义

\[
C_{th}=0.15\;\mathrm{kg/kg}
\]

并据此计算57.2745 h和51.0444 h。题目原文要求的是0.15 kg/kg，因此前文的1.5应明确更正为0.15，并检查摘要、表格和结论是否全部统一。

**影响：**研究目标、终止条件和最终时长在文字上不一致；若程序采用了1.5而正文采用0.15，结果将相差很大。

**建议：**全文统一使用0.15 kg/kg，并在首次出现处说明这是题目规定的干基含水率阈值。

### 2. 温度有限体积离散式漏掉体积热容

**位置：**式(9)。

论文先给出

\[
\rho c_p\frac{\partial T}{\partial t}
=\frac1r\frac{\partial}{\partial r}\left(rk\frac{\partial T}{\partial r}\right),
\]

但式(9)写成

\[
\frac{dT_i}{dt}=\frac{2}{r_{i,R}^2-r_{i,L}^2}[\cdots],
\]

右侧缺少 `1/(ρ cp)`。如果程序按该式实现，温度变化速率会被错误放大约 `ρcp` 倍；如果程序中实际有该项，则论文公式必须更正，否则无法复现。

**建议：**改为

\[
\frac{dT_i}{dt}=\frac{2}{(r_{i,R}^2-r_{i,L}^2)\rho_i c_{p,i}}[\cdots]
\]

并重新核验所有温度表和后续温度相关的扩散系数。

### 3. 变组成介质的能量方程不满足严格能量守恒

**位置：**式(1)、5.1.3、6.2。

论文使用随含水率变化的 `ρ(C)`、`cp(C)`，却仍采用

\[
\rho(C)c_p(C)T_t=\nabla\cdot(k\nabla T).
\]

当 `C` 随时间变化时，介质内部能量或焓的时间导数一般不能简单等同于 `ρ(C)cp(C)T_t`；至少需要说明采用了何种等效焓定义，或处理由 `C_t` 引起的储能变化。若考虑蒸发，还应讨论汽化潜热项。

**影响：**论文所谓“温度—水分双向强耦合”只是更新了系数，并不等于建立了完整的能量守恒模型；温度场和由温度决定的扩散系数可能存在系统偏差。

**建议：**从单位体积焓 `H(T,C)` 出发建立 `∂H/∂t = ∇·(k∇T)`；若缺少潜热参数，应明确这是简化模型并给出敏感性或误差讨论。

### 4. 干基含水率方程与真实水质量守恒不一致

**位置：**式(2)、5.2.3、6.2.3。

论文将 `C` 定义为干基含水率（kg水/kg绝干物质），却直接写

\[
C_t=\nabla\cdot(D\nabla C).
\]

若绝干物质密度或体积随干燥变化，守恒变量应是单位体积水质量，通常需要明确 `ρ_d C` 与扩散通量之间的关系，例如

\[
\frac{\partial(\rho_d C)}{\partial t}
=\nabla\cdot(\rho_dD\nabla C)
\]

或给出严格的等效定义及适用条件。当前方程没有说明为何可以忽略干物质密度和体积变化。

**影响：**问题一至问题三的水分场、累计失水量和问题四的收缩耦合都可能不一致。

### 5. 水分总量定义没有乘绝干物质权重

**位置：**式(8)、式(14)、式(15)。

论文把

\[
M(t)=\frac{2}{R^2}\int_0^R C(r,t)r\,dr
\]

称为“按绝干物质加权”的域内水分量，但该式实际没有出现绝干物质密度 `ρ_d` 或干物质质量权重。对于干基含水率，真实水质量应与绝干物质质量分布相乘后积分。

**影响：**由 `M(t)` 与表面流出量 `Q(t)` 得到的“守恒残差”并非严格的水质量守恒残差，不能据此证明模型守恒。

**建议：**明确基准量（单位长度绝干质量或初始体积），统一定义域内水质量、边界流出量及相对残差，并在固定域和收缩域分别推导。

### 6. 移动边界/收缩模型推导不充分，不能直接断言对流项精确相消

**位置：**6.4.1，式(19)–(21)。

论文先给出固相速度

\[
u_s=(r/R)\dot R,
\]

再将干基含水率写成简单的 Euler 对流扩散方程，并声称坐标变换项与固相迁移项“精确相消”。但该推导没有同时处理：

- 干物质密度或绝干质量坐标的定义；
- 体积雅可比和径向几何因子；
- 固相速度的散度项；
- 移动边界的相对通量条件；
- 干基含水率与单位体积水浓度之间的转换。

对干基变量而言，守恒方程通常不能仅靠给出一个速度项后直接约去所有收缩项。需要从水质量守恒和固相连续性方程重新推导物质坐标形式。

**影响：**问题四的核心理论依据不足；51.0444 h的时长可能是由不完整方程得到的数值，而非严格的收缩模型结果。

### 7. 移动边界处仍直接使用固定边界 Robin 条件

**位置：**式(5)、6.4.1。

论文在收缩问题中仍使用

\[
-D\,C_r=h_m(C_R-C_\infty),
\]

但移动界面的质量平衡应明确采用相对于界面或固相的通量，并说明边界运动是否携带水分。固定边界的通量条件不能未经推导直接照搬。

**建议：**写出移动边界上的 Stefan 型质量平衡或相对通量条件，并说明 `C_R` 是界面处材料变量还是固定空间变量。

### 8. 给定半径与含水率/密度没有通过统一方程耦合

**位置：**假设3、6.4.1、6.4.4。

论文一方面把附件2的 `R(t)` 作为外部输入，另一方面又用密度公式和绝干质量守恒“预测”半径。两者没有在同一个局部守恒系统中联立，因而只能算“给定边界下的水分扩散”，不能同时宣称半径由守恒律推导得到。

**影响：**附件2半径曲线与计算得到的含水率场可能并不相容；所谓“独立校验”不能替代一致性验证。

### 9. 式(22)用单一代表性含水率预测整体半径不成立

**位置：**6.4.4，式(22)。

收缩半径由全截面绝干质量和含水率分布决定，不能用“终态含水率量级”或单点含水率代入

\[
R_{pred}=R_0\sqrt{\frac{(1+C)\rho(C_0)}{(1+C_0)\rho(C)}}
\]

就得到整体半径。若 `C=C(r,t)` 非均匀，应对整个截面进行积分，再由总绝干质量守恒求体积或半径。

**影响：**3.29%、10.73%、21.22%的偏差比较缺少严格计算基础，不能作为“独立印证”。

---

## 三、数值方法与判据问题

### 10. “Crank–Nicolson 无条件稳定”表述过度

**位置：**2.1、6.1.1。

Crank–Nicolson 对线性扩散问题具有 A-稳定性，但不等于对非线性变系数问题无条件稳定，也不保证数值解非负或无振荡。论文没有给出非线性问题的稳定性、正性和时间步长收敛证据。

**建议：**报告时间步长加密结果、非负性检查和最大离散误差；必要时采用后向 Euler 或 θ 格式作为稳健性对照。

### 11. 问题一的“单向弱耦合”描述不准确

**位置：**2.1、5.1.3、7.2。

附录2中 `ρ`、`cp`、`k` 为常数，`D` 只依赖 `C` 且不依赖 `T`。因此温度方程和水分方程实际上是解耦的，而不是一般意义上的单向弱耦合。采用固定点迭代并非必要。

### 12. “中心恒为最湿点”缺少理论或充分数值证明

**位置：**2.3、6.3.1。

论文把全域最大含水率直接等同于中心含水率。对于非线性 `D(C,T)`、随时间变化的边界浓度及收缩边界，应通过最大值原理、单调性证明或逐时刻全域检查来支持该结论。文中只有口头说明“最湿点恒在中心”。

### 13. 终点插值只使用中心值，未严格对全域最大值插值

**位置：**式(17)、6.3.1。

如果终止条件定义为 `max_j C_j < C_th`，插值应基于相邻时间步的全域最大值；只有在已证明最大值始终位于中心时，才可使用中心值。论文没有把这一逻辑证明与插值实现完整对应起来。

### 14. 式(16)数学书写不完整

原文为 `max0 ≤ r ≤ R0 C(r,t)`，缺少下标和括号。建议规范写成

\[
t^*=\inf\{t>0:\max_{0\le r\le R_0}C(r,t)<C_{th}\}.
\]

### 15. Picard 迭代达到上限时的处理未说明

**位置：**6.3.1、式(18)。

论文写“迭代至收敛或达到轮数上限”，但没有说明未收敛时是否拒绝时间步、缩小步长、采用松弛，还是直接接受近似解。对末期低扩散系数导致的刚性问题，这一处理会显著影响时长。

### 16. 变系数界面采用算术平均缺少依据

**位置：**5.2.1。

当 `D` 或 `k` 在相邻单元间差异较大时，界面扩散系数通常需要由连续通量条件推导，常见选择是调和平均或距离加权等效系数。论文直接采用算术平均，却没有误差比较或物理依据。

### 17. 守恒残差数值表述矛盾

**位置：**摘要、5.2.3、6.2.3。

摘要声称“四问最大残差均不超过 `10^-12`”，正文又说3 h时残差为 `10^-6` 量级。两者不一致；此外，边界通量时间积分的求积方法和状态变量时间层没有说明，无法复核残差。

---

## 四、物理假设和解释问题

### 18. 一维径向假设的定量论证不足

**位置：**假设1。

论文称轴向扩散尺度约2.7 cm、端面影响约10%，但没有给出该尺度的计算公式和误差标准。若两端各受影响约2.7 cm，则相对于25 cm总长约为21.6%，不能直接称为“约10%”。需要给出二维模型对照或可接受误差界。

### 19. 忽略蒸发潜热却没有误差评估

**位置：**假设4。

干燥过程中存在大量水分蒸发，潜热可能显著影响药材温度。缺少参数可以作为简化理由，但不能在没有敏感性分析的情况下把温度和时长结果表述为高精度结论。

### 20. “收缩加速2.786倍”的解释不严谨

**位置：**6.4.3。

`1/R^2` 是随时间变化的系数，终态的 `(2/1.198)^2=2.786` 只能表示终态局部放大倍数，不能直接推出总干燥时长缩短倍数。总时长应由整个时间区间的积分效应和浓度依赖扩散系数共同决定。

### 21. “收缩效应压过扩散系数变小”证据不足

**位置：**6.4.3。

论文用冻结半径的另一次计算结果与真实收缩结果进行比较，但没有明确是否保持了相同网格、时间步、边界处理、温度场和终点判据，也没有给出误差条或重复计算。因此“2.54倍加速”的定量归因仍不充分。

### 22. 假设“长度不变、纯径向收缩、密度随含水率变化”缺少连续性约束

**位置：**假设2、假设3、6.4。

这三个条件并非自动相容。应通过固相连续性方程说明径向速度、密度变化和半径变化之间的关系，否则可能出现几何体积变化与绝干质量守恒不一致。

---

## 五、数据、结果和复现性问题

### 23. 题目要求的结果文件不完整

题目要求提交 `result1.xlsx`–`result4.xlsx`。当前工作目录中只有：

- `result1.xlsx`
- `附件1.xlsx`
- `附件2.xlsx`

没有发现 `result2.xlsx`、`result3.xlsx`、`result4.xlsx`。现有 `result1.xlsx` 的工作表仍含空值/NaN，未体现论文声称的每秒、每0.1 cm完整结果。

**影响：**论文结果无法按题目要求复核，也无法确认正文表格是否来自实际计算。

### 24. 经验公式未在正文中完整列出

正文多次引用“附录2、3、4物性”，但没有在论文中完整、规范地给出所有经验公式、参数、温度单位和适用范围。读者仅凭论文无法独立复现。

### 25. 灵敏度分析章节不完整

**位置：**7.3。

正文在“结果列于表11、绘于图31”后直接结束，表11和图31没有出现，结论、局限性、参考文献和附录也缺失。因此“扩散系数与几何尺寸主导、对流换热系数几乎无影响”等结论没有证据支撑。

### 26. 图号和交叉引用混乱

问题二、问题三均引用“图16”；正文图片仍带有占位符路径；图19、图20、图21等编号与前文图片编号不完全一致。建议重新统一图表编号，并检查所有引用是否指向正确对象。

### 27. 表格数值存在不一致

例如问题一表格/摘要中表面含水率出现1.5102、1.5104等不同数值；摘要、正文、结果汇总表的精度和四舍五入规则也未统一。应明确原始值、显示值和终点插值值的关系。

### 28. 问题四表格的空间坐标含义不明确

表7中的“0.5 cm、1 cm”等列没有说明是固定物理半径坐标，还是随收缩变化的物质坐标 `η` 映射位置。收缩过程中两种坐标不是同一个位置，必须明确，否则表格无法解释和复现。

### 29. 公式和交叉引用存在排版错误

- 6.4.1中“代入(16)”应为代入式(20)；
- 式(19)中的 `\dot R` 排版丢失；
- 式(21)中的变量和边界条件未完整列出；
- 个别公式编号、图号和题目表号混用。

---

## 六、建议的修改顺序

1. 全文统一阈值为 `0.15 kg/kg`，修正问题重述、摘要、判据和结论。
2. 修正式(9)的 `ρcp` 漏项，并核验程序实现与论文公式一致。
3. 重新定义干基含水率对应的守恒变量、绝干质量权重和水分质量残差。
4. 从焓守恒重新审查变含水率温度方程，并说明是否忽略潜热及其误差。
5. 重新推导移动边界问题：固相连续性、物质坐标、雅可比、移动边界相对通量和半径约束必须统一。
6. 重新计算问题三、问题四的时长，并给出网格、时间步和非线性迭代收敛表。
7. 用全域最大含水率实施终点判据；若使用中心值插值，必须先证明中心始终为最大值。
8. 补齐 `result1.xlsx`–`result4.xlsx`，并检查每个文件的时间、空间网格和四位小数格式。
9. 补全灵敏度分析、结论、模型局限、参考文献、经验公式及所有图表。

## 七、总体评价

论文的建模思路（轴对称径向扩散、有限体积离散、变物性迭代、移动坐标尝试）具有一定合理性，但当前版本在守恒变量、变密度能量方程和收缩边界推导方面存在基础性缺陷。尤其是式(9)漏项、干基水分守恒定义不一致和问题四移动边界推导不充分，会直接影响核心数值结果。完成上述理论修正和完整复算之前，不建议将57.2745 h和51.0444 h作为最终可信结论。
